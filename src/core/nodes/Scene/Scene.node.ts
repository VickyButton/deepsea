import { Node } from "@core/components/Node.component"

export interface SceneState {
    title: string
}

/**
 * A node that acts as a root for layer nodes.
 */
export class Scene extends Node {
    public start() {

    }

    public update(dt: number) {

    }
}